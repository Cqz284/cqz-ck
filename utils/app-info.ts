/**
 * 应用信息常量
 *
 * 版本号没有运行时来源（小程序不读 package.json），因此集中在这里，发版时改这一处即可。
 */
export const APP_NAME = '记账记事';

/** 当前版本号 */
export const APP_VERSION = '1.0.0';

/** 展示用版本文案，如 v1.0.0 */
export const APP_VERSION_TEXT = 'v' + APP_VERSION;
